{
  "name": "@app/db",
  "version": "0.0.0",
  "private": true,
  "description": "Schéma Drizzle, migrations SQL, politiques RLS, seeds synthétiques. Vide au lot L00 (base applicative en L01).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "24.13.6",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
